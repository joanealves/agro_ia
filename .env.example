# ============================================================================
# AGROÍA - CONFIGURAÇÃO DE AMBIENTE (DESENVOLVIMENTO)
# ============================================================================
# 
# Copie este arquivo para .env e preencha com seus valores reais:
#   cp .env.example .env
#
# NUNCA commite o arquivo .env com credenciais reais no repositório!
# ============================================================================

# ============================================================================
# DJANGO
# ============================================================================

# Gere uma nova chave segura:
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-gere-uma-nova-chave-segura-aqui

# True em desenvolvimento, False em produção
DEBUG=True

# URLs permitidas (separe por vírgula)
ALLOWED_HOSTS=localhost,127.0.0.1,*.localhost

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ============================================================================
# BANCO DE DADOS (SUPABASE PostgreSQL)
# ============================================================================
# 
# Obtenha estes valores em: supabase.com -> Project Settings -> Database
#

# Sempre PostgreSQL para produção
DB_ENGINE=django.db.backends.postgresql

# Nome do banco (padrão: postgres)
DB_NAME=postgres

# Seu usuário Supabase (ex: postgres.jslkdjlkajsd)
DB_USER=postgres.your_supabase_user_here

# Sua senha do Supabase
DB_PASSWORD=your_supabase_password_here

# Host do Supabase (ex: aws-1-us-east-1.pooler.supabase.com)
DB_HOST=your_supabase_host_here

# Porta padrão Supabase
DB_PORT=6543

# ============================================================================
# SEGURANÇA - COOKIES E CSRF
# ============================================================================

# Em produção com HTTPS, mude para True
AUTH_COOKIE_SECURE=False

# Lax em dev, Strict em produção
AUTH_COOKIE_SAMESITE=Lax

# ============================================================================
# CELERY (Tasks Assincronas)
# ============================================================================

# Redis local (dev) ou Redis em produção
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# ============================================================================
# OPTIONAL: APIs Externas (Fase 2+)
# ============================================================================

# Open-Meteo (gratuito, sem API key necessária)
OPEN_METEO_API_URL=https://api.open-meteo.com/v1/forecast

# AWS S3 (para uploads de imagens)
# AWS_ACCESS_KEY_ID=your_aws_key_here
# AWS_SECRET_ACCESS_KEY=your_aws_secret_here
# AWS_STORAGE_BUCKET_NAME=your_bucket_name

# ============================================================================
# LOGGING
# ============================================================================

LOG_LEVEL=INFO
